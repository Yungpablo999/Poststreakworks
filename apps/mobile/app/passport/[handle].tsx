// ============================================================================
// Public Creator Passport (mobile)
// STUB — structural placeholder only. No implementation yet.
// Mirrors: apps/web/app/passport/[handle]/page.tsx
// Governed by: apps/web/FRONTEND_ARCHITECTURE.md
// ============================================================================
// Same intent as the web version. Note: a public, unauthenticated route is
// an unusual pattern inside a native app (there's no "just visit a URL"
// equivalent without the app installed) — this screen is really for
// in-app viewing of another creator's Passport; the actually-shareable
// public link is the web route. Worth confirming this split makes sense
// once real sharing flows are designed.
// ============================================================================
