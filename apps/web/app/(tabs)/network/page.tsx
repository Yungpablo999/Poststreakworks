// ============================================================================
// Network
// STUB — structural placeholder only. No implementation yet.
// Governed by: apps/web/FRONTEND_ARCHITECTURE.md
// ============================================================================
//
// Intended contents: the four Stage-1-confirmed Creator Network pillars,
// nested under this one tab rather than each getting a top-level route:
//   - Discover / Match (discovery feed, swipe-style actions)
//   - Collaboration Workspace (a match's chat + brief, once matched)
//   - Squads (city/niche/stage/language groups)
//   - Quests (doc2's "shared quests" concept — nested here, not Grow;
//     revisit if that placement turns out wrong)
//   - Duels (two-person streak-accountability pairing — added during
//     Stage 5, see packages/workflows/duel-lifecycle.ts)
//
// Creator Passport is NOT here — it's a public-facing route
// (app/passport/[handle]/page.tsx) since it must be viewable by people
// without an account. This tab holds the creator's own edit/preview view,
// which links out to the public route.
// ============================================================================
