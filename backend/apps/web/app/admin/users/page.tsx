export default function AdminUsersPage() {
  return (
    <div style={{ padding: 24, fontFamily: "system-ui" }}>
      <h1>Users</h1>
      <p>Search/verify/suspend/restrict/warn — not built yet. Real gate is live (see app/admin/layout.tsx); this body is a placeholder.</p>
    </div>
  );
}
