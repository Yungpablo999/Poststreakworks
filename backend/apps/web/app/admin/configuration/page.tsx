export default function AdminConfigurationPage() {
  return (
    <div style={{ padding: 24, fontFamily: "system-ui" }}>
      <h1>Configuration</h1>
      <p>Feature flags (DB-backed, per BACKEND_ARCHITECTURE.md item J) — not built yet.</p>
    </div>
  );
}
