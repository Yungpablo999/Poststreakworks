// ============================================================================
// Sign in / sign up
// STUB — structural placeholder only. No implementation yet.
// Governed by: apps/web/FRONTEND_ARCHITECTURE.md
// ============================================================================
//
// Intended contents: Supabase Auth UI — email/phone + social identity
// provider sign-in and sign-up, combined in one stub for now (may split
// once real auth flows are built). Web uses redirect-based OAuth; the
// mobile equivalent (apps/mobile/app/(auth)/index.tsx) needs a native
// deep-link/in-app-browser flow instead — same backend session model
// (packages/api/context.ts dual auth), different client-side mechanics.
// ============================================================================
